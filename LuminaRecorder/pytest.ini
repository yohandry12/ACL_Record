[pytest]
; La suite vit dans tests/ — sans ce cadrage, pytest partait collecter
; les tests internes de PyTorch dans dist_installer/app/_internal/
; (l'application empaquetée en mode dossier) et échouait sur leurs
; dépendances de développement absentes.
testpaths = tests
norecursedirs = dist dist_installer build .git
